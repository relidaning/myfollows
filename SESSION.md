# Sessions

## 2026-09-18 — Auto-repeat playback, header/nav consolidation, unfollow-persists bug (fixed), unlisted-channel sidebar filter
Added auto-repeat for the currently playing video (`loop` attribute for the native Douyin `<video>`; `loop=1&playlist=<id>` for the YouTube embed, since `loop=1` alone loops the "up next" queue instead of the single video) and redeployed. Then, after using `AskUserQuestion` to disambiguate a bundled multi-part request, reworked `douyin-mcp/ui.html`'s header: moved the label/platform filters, "show filtered", "show unlisted", and "fix old links" into the existing gear-icon dropdown panel (now behaves the same on desktop as it already did on mobile, rather than sitting inline only on desktop); merged "Sync Douyin"/"Sync YouTube" into one `Sync` button that runs both in sequence; merged Douyin's automatic-QR-only login and YouTube's manual login button into one `Relogin` button that fires both platforms at once (per user's choice of "one click, both at once"); added a client-side search box filtering the video grid and creator sidebar by title/name (no extra server round trips); and sorted unlisted creators to the tail of `/api/creators` in `server.py` while preserving existing ordering otherwise. Verified via curl against the rebuilt container.

Root-caused the "unfollowed channel still appears" report: called the real `/api/videos/{id}/unfollow_creator` endpoint directly against a live Douyin creator (不娘家) and confirmed the automated unfollow genuinely fails (`button:has-text('已关注')` couldn't find the follow button — the selector this repo's own notes already flagged UNVERIFIED in 2026-07-29 is now confirmed stale), so the old code's "only purge locally on live success" logic never ran. Fixed by decoupling the two: added a `blocked_creators` table plus `common.block_creator()` (deletes the creator's row and all their videos, and records them so `upsert_creators`/`upsert_videos` skip them on every future sync regardless of whether the live unfollow ever succeeds); `/api/videos/{id}/unfollow_creator` now always purges and reports `live_unfollowed` separately so the UI can warn the user to unfollow manually on the real platform if that part failed. Manually purged 不娘家 and 温莎饰家 (32 videos) through this path per user request and verified via curl that neither appears in `/api/creators` or `/api/videos` anymore, even with `show_unlisted=true`.

Per follow-up request, also hid unlisted creators from the sidebar by default (previously they were always shown, just dimmed and sorted to the tail) — `renderCreatorList` in `ui.html` now filters them out unless `#showUnlisted` is checked, reusing the same checkbox that already gated their videos in the grid. Rebuilt and redeployed; a user report of "unlisted channels still showing" right after that redeploy was diagnosed as a stale already-open tab (served HTML/JS verified correct via curl) rather than a real bug — told the user to hard-refresh.

Known follow-up, not yet actioned: Recommended/Sync/Relogin/checkboxes now stay in the main header row at every screen width, which can wrap to a taller header on narrow phones. Also: the underlying Douyin unfollow selector itself is still broken (confirmed above) — the app-side purge works regardless, but a genuinely followed-on-Douyin creator whose automated unfollow fails needs to be unfollowed by hand on Douyin too; fixing the selector needs a real followed profile page inspected with devtools.

## 2026-09-15 — Diagnose YouTube "not a bot" wall as VPN-specific, revert attempted fix, push backlog
User reported the YouTube embed "sign in to confirm you're not a bot" interstitial (a known, already-worked-around limitation per `CLAUDE.md`) firing on almost every video and surviving retries; first added a silent one-time auto-retry to `armYtFallbackTimer` in `douyin-mcp/ui.html` and redeployed, but the user said it still didn't help. Follow-up questioning (device, then VPN/proxy) traced the real cause to the user's **full-tunnel VPN's exit IP** being flagged by YouTube's embed-only bot detection — the same IP loads youtube.com fine directly, since that stricter check applies only to anonymous iframe embeds, so every retry hits the same flagged IP and fails identically; no app-side fix exists, only client-side VPN split-tunneling for `youtube.com`. Reverted the auto-retry edit back to the last-committed behavior per user request and redeployed. Also investigated a "grid is empty" report (server-side data/rendering checked out fine via curl) which turned out to be a stale browser tab left open across the container restart, not a bug. Finally, per user request, committed and pushed the four-file backlog of already-tested work that had been sitting uncommitted across prior sessions (`CLAUDE.md`, `SESSION.md`, `douyin-mcp/ui.html`, `douyin-mcp/youtube.py` — real-phone swipe-over-YouTube-iframe fix and channel unsubscribe selector fix) as `4315f30`, pushed to `origin/master`.

## 2026-08-10 — Declined out-of-scope "claude-maxer" vault task (unattended vault-tasks run, no changes)
Ran unattended off an Obsidian `Tasks.md` entry asking to "optimize the claude-maxer, utilize the 5h window and consume the token quota"; searched the repo (`grep -ril claude-maxer`, `git log --all` for "maxer"/"token", README/SESSION/CLAUDE) and found zero references — the task describes how the agent/scheduler itself should pace Claude token usage across a 5-hour rate-limit window, not a feature of this video-follows sync app. Stopped without writing code or committing rather than fabricating an unrelated subsystem inside `myfollows`, and left the pre-existing uncommitted changes (`CLAUDE.md`, `SESSION.md`, `douyin-mcp/ui.html`, `douyin-mcp/youtube.py`, `.claude/`) untouched; noted that if "claude-maxer" is a separate tool/repo the task should be pointed there instead.

## 2026-08-10 — Redeploy verification, recommendation algo explainer, and playback swipe-on-phone fixes
Confirmed the running container was one commit stale (missing the Recommended Top 40 feature) plus had uncommitted `youtube.py` changes, rebuilt and redeployed (`docker compose build && up -d --force-recreate`), then explained the `/api/videos/recommended` scoring algorithm and the underlying `_classify_labels` keyword/substring tagging mechanism (no LLM involved) to the user. User reported the gated scroll/swipe navigation (already shipped in `24d1d05`) wasn't working on their phone, and the close button overlapped the layout; diagnosed two separate real-phone-only bugs across two redeploys: (1) YouTube's cross-origin embed iframe swallows touch/wheel events before they reach the parent page's swipe listeners — fixed by adding a transparent `#playerYtCapture` div on top of the iframe that intercepts touches and re-dispatches tap-to-pause; (2) even outside the iframe, the browser's own gesture recognizer can claim a vertical drag as a system gesture (pull-to-refresh) and fire `touchcancel` instead of `touchend`, which nothing was listening for — fixed by adding `touch-action: none` to `#playerOverlay` plus a `touchcancel` reset handler. Also restyled/relocated `#playerClose` per the user's answers to two clarifying questions. Session ended immediately after the second redeploy, before the user confirmed on-phone whether swiping now actually works — treat as unverified.

## 2026-08-10 — Add Recommended Top 40 (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry asking for a way to cut an overwhelming unwatched backlog down to a manageable, personalized slice; added `GET /api/videos/recommended` in `douyin-mcp/server.py`, which ranks unwatched/non-filtered/non-unlisted videos by a blended score (`interest_score×2` + 3 if starred + 2 if watch_later + average `interest_score` of *other* already-scored videos sharing a label, so affinity generalizes to never-rated videos) and returns the top `limit` (default 40), falling back to recency on ties so it's useful even on a fresh, unmarked DB — deliberately built from existing metadata rather than adding an ML/LLM dependency. Wired a `🎯 Recommended` header toggle into `douyin-mcp/ui.html` that swaps the grid to this ranked view and disables the now-irrelevant filter controls while active (selecting a creator exits recommended mode, since the creator filter doesn't apply to the ranked pool). Verified via `python3 -m py_compile`, `node --check` on the extracted inline script, and a live `docker compose build && up -d --force-recreate` round-trip hitting the real endpoint (confirmed 40 ranked results with real scores). Committed directly to master (`35f28e1`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `SESSION.md`, `youtube.py`, `.claude/`) untouched.

## 2026-08-10 — Disable native video control panel on the playback page (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry asking to hide the browser's native video control overlay (ff/rw 10s, pause, fullscreen, volume) that appeared on tap; removed the `controls` attribute from `<video id="playerVideo">` in `douyin-mcp/ui.html` since the app already has its own tap/keyboard-driven play/pause, seek, and fullscreen handling, making the native panel purely redundant. No lint/build tooling exists for this static file, so none was run, and no container rebuild was done this session (left as a follow-up for the change to take effect on the live app, per this repo's no-bind-mount rule). Committed directly to master (`5d334b7`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `SESSION.md`, `youtube.py`, `.claude/`) untouched.

## 2026-08-10 — Detect and gracefully fall back when YouTube blocks embed playback (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry reporting "sign in to confirm you're not a bot" during YouTube video playback; root cause is YouTube's `youtube.com/embed/{id}` iframe silently substituting that interstitial for the real player — a cross-origin HTML page invisible to our JS with no `error` event, so playback previously just hung with no feedback. Added `armYtFallbackTimer` in `douyin-mcp/ui.html`: `openPlayer` and the retry path now arm a 9s timer that shows the existing playback-fallback UI (Retry / "Open on YouTube") unless a postMessage arrives from the iframe first, since the real player starts broadcasting almost immediately and the interstitial never does — reused the fallback overlay that already existed for the Douyin native-`<video>` error case rather than building a new one. Documented the mechanism in README (can't be fixed server-side; it's YouTube's own bot detection on the viewer's IP). Verified via `node --check` on the extracted inline script only (no container rebuild this session). Committed directly to master (`894e2a7`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `SESSION.md`, `youtube.py`, `.claude/`) untouched.

## 2026-08-09 — Diagnose and fix 35s+ first-page-load stall (fix since lost, not in current code)
Diagnosed why the page took 35s+ (sometimes 60-90s) to show anything on first load: `ui.html`'s `init()` awaited `checkLogin()` before calling `loadVideos()`/`loadCreators()`, and `checkLogin()` hits `/api/status`, which — like `/api/youtube/status` — blocks on the same global `asyncio.Lock`/`_headless_lock` that the startup/scheduled full sync holds for its *entire* duration (confirmed via container logs: 74s Douyin sync + 16s YouTube sync back-to-back on one boot), even though `loadVideos()`/`loadCreators()` are pure local-DB reads with no browser/lock involvement. Fixed by decoupling: `checkLogin()` no longer triggers the video/creator loads itself, `init()` fires `loadVideos()`/`loadCreators()` immediately and runs `checkLogin()` + `initYoutubeStatus()` concurrently via `Promise.all`. Verified live via `docker compose build && up -d --force-recreate` plus curl timing during an active startup sync (`GET /` 2ms, `/api/videos` 25ms, `/api/status` still 62s as expected but no longer blocking anything else). **This edit was never committed** — session ended without a commit, and by the very next session's start (~23 min later, `8fdb2ae`) `ui.html` already had zero uncommitted diff, so the fix was lost somewhere in between; current `douyin-mcp/ui.html` still has the old blocking `checkLogin()`/sequential `init()`. The 35s+ first-load stall is very likely still present — redo this fix if it resurfaces.

## 2026-08-09 — Session-logging documentation run (meta)
Read a prior unattended vault-tasks transcript (the gated scroll/swipe navigation feature) and appended its SESSION.md entry (now the top entry below); judged no CLAUDE.md update was warranted since that session followed existing bind-mount-rebuild and port-collision-check patterns with no new mechanism. This entry documents that documentation run itself, per the same recurring housekeeping task.

## 2026-08-09 — Add gated scroll/swipe navigation between videos on the playback page (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry ("make video playing scrallable, but only available after tagged and scoreed."); added wheel-scroll and vertical touch-swipe handlers to the player in `douyin-mcp/ui.html` that move to the next/previous video in the currently loaded list, TikTok-feed style, but advancing to the **next** video is blocked until the currently open one has both a score and at least one label — attempting to scroll forward before that shows a "Rate & tag to continue ↑" nudge and opens the rate/tag sheet instead; going back is never gated. Verified via `node --check` on the extracted inline script and a full `docker compose build && up -d --force-recreate` (confirmed no port collision on 8082 and the served page contains the new JS). Committed directly to master (`24d1d05`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `SESSION.md`, `youtube.py`, `.claude/`) untouched.

## 2026-08-09 — Move source link right after title on video playback page (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry ("I found a link in video playing page: open on xx, move it after the title."); in `douyin-mcp/ui.html`'s `#playerInfoOverlay`, moved the "Open on YouTube/Douyin ↗" `#playerLink` element to sit directly under the title, above the channel-name/date row (previously it was last), and updated the adjacent CSS comment describing the stacking order to match. No lint/build tooling exists for this static file; verified only via a div open/close tag count on the file (no container rebuild). Committed directly to master (`2cdc630`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `SESSION.md`, `youtube.py`, `.claude/`) untouched.

## 2026-08-09 — Default video playback speed to 1.75x (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry ("set the speed for video playing, default as 1.75x"); added `DEFAULT_PLAYBACK_RATE = 1.75` in `douyin-mcp/ui.html`, applied via `playerVideo.playbackRate` for Douyin's native `<video>` and via a `setPlaybackRate` postMessage command (sent once the YouTube iframe's `load` event fires, matching the existing volume/play/pause command pattern) for the YouTube embed; also updated README's keyboard-shortcuts note, which previously said speed control was deliberately left to an external browser extension — now stale. Verified only via `node --check` on the extracted inline script (no container rebuild). Found pre-existing uncommitted changes already sitting in `CLAUDE.md`, `SESSION.md`, and `youtube.py` from earlier sessions, so isolated just this task's diff (via HEAD-vs-working-tree text splicing in `/tmp`) before staging/committing, to avoid folding unrelated work into the commit; committed directly to master (`8fdb2ae`) per task instructions, without pushing.

## 2026-08-08 — Clear phone status bar from the top header on mobile (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry reporting that on mobile the top elements were covered by the phone's time/signal/battery status bar; root cause is this being a PWA (`apple-mobile-web-app-capable` + `viewport-fit=cover`), which draws content under the status bar by default unless a view opts out with `env(safe-area-inset-top)` — the video player overlay already did this, but the sticky `header` and the off-canvas creator drawer in `douyin-mcp/ui.html` didn't. Fixed by padding both with `max(<existing-padding>, env(safe-area-inset-top))` (header, both desktop and the `@media (max-width: 860px)` variant) and `padding-top: env(safe-area-inset-top)` on the drawer. No lint/build step exists for this static file; verified only via a brace-balance check on the extracted `<style>` block (no container rebuild/screenshot verification this time). Committed directly to master (`42bf816`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `README.md`, `SESSION.md`, `youtube.py`) untouched.

## 2026-08-08 — Redesign playback page: full-viewport, TikTok-style layout (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry asking for a full-viewport player with title/channel/date moved below it, a playback progress bar, hoisted star/watch-later/tag buttons, and TikTok-style fluid text sizing; reworked `douyin-mcp/ui.html`'s player CSS/HTML/JS via precise line-range Python splicing (`#playerOverlay`/`#playerCard` now always fill `100vw × 100dvh`, a bottom-anchored `#playerInfoOverlay` gradient scrim holds title/`@channel · date`/source link, a new `#playerProgressTrack`/`#playerProgressFill` bar is driven by the native `<video>`'s `timeupdate` event — flat for YouTube's cross-origin iframe, which can't report progress — and text sizes switched to `clamp()` with a `vw` component). Verified via `node --check` on the extracted script, a tag/brace balance check, and real Playwright screenshots (mobile 390×844, desktop 1400×900) against the static file with mocked API routes rather than a full container rebuild (no live container round-trip was done, unlike this repo's usual bind-mount-free rebuild step); committed directly to master (`847ddd9`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `README.md`, `SESSION.md`, `youtube.py`, `.claude/`) untouched.

## 2026-08-08 — Remove unwatched red-dot indicator from video list cards (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry ("remove the red dot on the top left of video list page"); removed the `.unwatched-dot` badge in `douyin-mcp/ui.html` — its CSS rule plus the two JS references that rendered it in `card()` and toggled it in `updateCardInPlace()` — leaving the rest of the unwatched/watched tracking logic untouched. No lint/build tooling exists for this static file, so verified via a Node syntax check of the extracted inline script only; committed directly to master (`a7e2953`) per task instructions, without pushing, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `README.md`, `SESSION.md`, `youtube.py`) untouched.

## 2026-08-08 — Make the viewport non-scalable (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry ("make the viewport non-scalable"); added `maximum-scale=1, user-scalable=no` to the viewport `<meta>` tag in `douyin-mcp/ui.html` to disable pinch/double-tap zoom. No lint/build tooling exists for this static HTML file, so none was run; committed directly to master (`5565618`) without pushing, per task instructions, leaving other pre-existing uncommitted changes (`CLAUDE.md`, `README.md`, `SESSION.md`, `youtube.py`) untouched.

## 2026-08-08 — Fix new custom tags not showing up on other videos until reload (unattended vault-tasks run)
Ran unattended off an Obsidian `Tasks.md` entry reporting that a newly-typed tag didn't appear when the next video was opened; a background Explore agent traced the root cause to `douyin-mcp/ui.html`'s `allLabelCategories` (the in-memory list powering the "Rate & tag" chip picker), which was only ever populated once per page load behind a `labelFilterPopulated` guard in `loadVideos()` — the add-tag handler pushed new tags onto the current video's own `labels` array and POSTed them, but never updated the shared list, so it stayed stale until a full reload (server-side `/api/videos` was already returning a correct, up-to-date `label_categories` on every call). Fixed by also pushing newly-created tags into `allLabelCategories` (case-insensitive dedup) at the point they're added. Verified only via `node --check` on the extracted inline script (no bind-mount/rebuild step was run, since restarting the live container was judged out of scope for an unattended fix); committed directly to master (`5651104`) per task instructions, without pushing.

## 2026-08-08 — Hide redundant close button in Rate & tag sheet (unattended vault-tasks run)
Ran unattended via the vault-tasks scheduled routine off an ambiguous Obsidian `Tasks.md` entry ("I want hidden the 'x' button"); investigated all three ✕ close buttons in `douyin-mcp/ui.html` with headless-Playwright screenshots and code, left the video player's ✕ alone (its only close mechanism on mobile — no backdrop-click, no swipe), and hid `#playerSheetClose` in the "Rate & tag" bottom sheet since it's redundant with the existing tap-outside-to-dismiss backdrop. Found the prior session's mobile-redesign work still uncommitted in `ui.html`, so committed that pending work first as its own commit (`528ca92`, matching SESSION.md's existing description of it) before landing the scoped ✕-hide change separately (`86d215d`), keeping the task-specific diff isolated; verified via rebuild+recreate (no bind-mount) and a headless browser check that the sheet still dismisses via backdrop tap. Nothing pushed to remote, per task instructions.

## 2026-08-08 — Mobile-friendly UI: responsive layout, TikTok-style player rail, scroll-lock fix
Reshaped `douyin-mcp/ui.html` for phone-browser use (app already listened on `0.0.0.0:8082`, no server change needed): collapsed the header into a 👥/⚙ icon pair with an off-canvas creator drawer and a filters/sync dropdown on narrow screens, then per follow-up requests replaced the player's always-visible score/tag/unfollow controls with a TikTok-style bottom-right action rail (★ star, 🕓 watch later, 🏷 tag icon opening a bottom sheet with score/labels/unfollow/unlist) and fixed a body-scroll-lock bug where touch-scrolling the open player/sheet leaked through to the video list underneath. Diagnosed a reported "An error occurred (Playback ID: ...)" YouTube error as YouTube's own anti-bot wall reacting to the host's network/IP reputation (reproduced directly hitting `youtube.com/embed/...` without the proxy `youtube.py` uses internally) — not a bug in this app, since the embed iframe talks to YouTube directly from the browser, unproxied. Verified all changes with headless Playwright screenshots at mobile and desktop viewports after each `docker compose build && up -d --force-recreate` (per this repo's no-bind-mount rule); also resolved a user-reported WireGuard-IP access issue as a host/peer routing question outside this app's control (this host is a WireGuard client of a hub, not a server the phone can dial directly).

## 2026-08-07 — Add per-creator Unlist feature
Added a local-only, reversible "Unlist" flag on creators (new `creators.unlisted` column + migration in `common.py`, `POST /api/creators/unlist` toggle + `show_unlisted` filter on `GET /api/videos`/`GET /api/creators` in `server.py`) that hides a creator's videos from the feed without touching the real Douyin follow / YouTube subscription — deliberately distinct from the existing destructive Unfollow/Unsubscribe action. Wired the toggle into three UI surfaces (sidebar row, channel-filter header bar, video player dialog) all staying in sync, then per follow-up requests moved the player's Unlist button next to Unfollow and added a channel name/badge line to the player. Verified end-to-end with a headless Playwright browser after each rebuild (`docker compose build && up -d --force-recreate`, per this repo's no-bind-mount rule); noticed partway through that the user was already live-testing the sidebar button in their own browser, so testing was done against an untouched creator to avoid clobbering real data. Committed and pushed as `2895af7`.

## 2026-08-07 — Fix player dialog closing on outside click
Removed the `playerOverlay` backdrop click-to-close listener in `douyin-mcp/ui.html` so the video player dialog now only closes via the ✕ button or Esc, per user request (accidental dismissal while tagging/scoring a video). The code edit alone didn't take effect because the container doesn't bind-mount app source (`docker-compose.yml` only mounts `./data`) — `ui.html`/`server.py`/etc. are baked into the image at build time — so `docker compose build && docker compose up -d --force-recreate` was needed to actually pick up the change.

## 2026-07-30 — Scheduled daily 6am sync
Added `_scheduled_sync_loop` to `douyin-mcp/server.py`: a daemon thread started from `__main__` that sleeps until `SYNC_HOUR` (default 6) container-local time and then POSTs to its own `/api/sync` and `/api/youtube/sync` over loopback. Chose a plain thread + loopback HTTP over an asyncio task or a cron/APScheduler dependency so it needs no hook into FastMCP's event-loop startup and inherits the REST endpoints' existing locking and not-logged-in handling; verified the container comes up clean and its clock is Asia/Shanghai, and documented it in README under "Scheduled daily sync". The session then hit its usage limit on a follow-up bug report — clicking anywhere while a video is playing closes the player dialog, which should only close on Esc — so that fix is still open.

## 2026-07-29 — Replace VNC-based YouTube login with a real desktop Chromium window
Removed the in-container Xvfb+x11vnc+noVNC+matchbox virtual-display stack (deleted `douyin-mcp/start.sh`, stripped VNC packages/ports from `Dockerfile`/`docker-compose.yml`) and replaced it with mounting the host's `/tmp/.X11-unix` socket + `DISPLAY=:0` into the container, so clicking "Login to YouTube" launches a headed Chromium that pops up directly on the host desktop — chosen over a real Google OAuth app (rejected: would require a Google Cloud project, drop the subscriptions-feed scraping approach for the Data API, and force weekly re-auth without app verification). Updated `youtube.py`, `server.py`, `ui.html`, README, and CLAUDE.md accordingly; this is now documented as "Option A" in README alongside the existing cookie-import path ("Option B") from the prior session. Session ended mid-`docker compose up -d --build` verification, so the rebuild/end-to-end test wasn't confirmed complete within the transcript.

## 2026-07-29 — YouTube cookie-import login, first real sync fixes, container port collision
Replaced the noVNC-driven YouTube login flow's embedded modal with a proper new-tab open, then (per user request to avoid VNC/remote-control entirely) built `scripts/import_youtube_cookies.py` to lift live session cookies from the user's local Chrome (decrypted via the Secret Service D-Bus keyring) into `data/youtube_storage_state.json`, plus a `/api/youtube/reload_session` endpoint so the running container picks up a re-imported session without a restart. This was the first real end-to-end YouTube sync ever attempted, which surfaced several pre-existing bugs: a `pw.chromium.new_context()` vs `browser.new_context()` typo, stale DOM selectors from a YouTube redesign (`_scrape_subscriptions`), unbounded scrolling that loaded hundreds of cards regardless of `limit` (causing multi-minute hangs/OOM-style restarts), a stale `/tmp/.X99-lock` blocking Xvfb on in-place container restarts, and an unrelated second container (`douyin-follows-douyin-mcp-1`, a different project) squatting host port 8082 via `network_mode: host` and intermittently answering this app's requests — stopped after user confirmation. Confirmed a full sync (90 videos, 33 creators, ~12.6s) working end-to-end afterward; README's "YouTube login" section now documents cookie-import as "Option B" alongside the VNC flow.
